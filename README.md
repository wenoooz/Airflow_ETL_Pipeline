# Airflow_ETL_Pipeline
An Apache Airflow pipeline that runs a set of reproducible Sionna simulations, collects raw outputs, transforms them into a clean analytics dataset, stores the dataset, and generates a short KPI report.
